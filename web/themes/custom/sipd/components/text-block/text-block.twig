{% set alignment_class = text_align ? 'text-align-' ~ text_align : '' %}
<article class="text-block {{ alignment_class }} {{ text_block_classes|join(' ') }}"{{ attributes }}>
  {% if title %}
    <h2 class="text-block__title">{{ title }}</h2>
  {% endif %}   
   <div class="text-block__body">
        {{ body }}
   </div>
</article>