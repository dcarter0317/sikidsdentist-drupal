{#
/**
 * @file field--field-ept-columns.html.twig
 *
 * Theme override for field_ept_columns.
 * Restores classes so that the ept_columns CSS grid layout functions correctly
 * in a stable9-based theme.
 */
#}
<div class="field field--name-field-ept-columns">
  {% for item in items %}
    <div class="field__item">{{ item.content }}</div>
  {% endfor %}
</div>
